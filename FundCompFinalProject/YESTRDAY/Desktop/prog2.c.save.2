#include <stdio.h>
int main ()
{
 int touchdowns;
 int extra;
 int safeties;
 int field;
 int score;

  printf("How many touchdowns?\n");
  scanf("%d", &touchdowns );

  printf("How many extra points?\n");
  scanf("%d", &extra );

  printf("How many safeties?\n")"
  scanf("%d", &safeties );

  printf("How many field goals?\n");
  scanf("%d", &field );

  score = (7 * touchdowns) +(3 * field) + (extra) + (2 * safeties);
  printf("Score is %d\n", score);

}

